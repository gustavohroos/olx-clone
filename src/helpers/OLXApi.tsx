

const OLXApi = () => {

    login: async (email:string, password:string) => {
        // let { data:json } = await OLXApi.post(`/auth/login`, {email, password})
        return true;
    }
}

export default ()=>OLXApi;